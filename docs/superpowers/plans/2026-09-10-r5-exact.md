# R5 exact evidence implementation plan

Spec: C:/Users/owjs3/orca/devup-mcp-briefs/R5-exact-must-mean-exact.md

Authorized scope: R5 on PR #26, same release. Execute inline in this existing worktree.

- [x] Replay the WQUW-119 raw snapshot in folded_asset_size tests; assert 320x48 source dimensions, responsive FILL/aspectRatio and dimension provenance. Add corrupted-output and missing-geometry cases before fixes.
- [x] Repair only child-folded SVG mask HUG axes in codegen/layout.rs; preserve FILL with ratios and exclude unsupported transforms. Validate actual positive size evidence in provenance.rs before coverage exemptions. Reuse output-scoped layout loss diagnostics.
- [x] Audit remaining fidelity categories and document limits and additional findings outside the repository.
- [x] Add an offline asset batch merge CLI consuming saved export/job responses; deduplicate assets, reject mixed source versions, expose failures/unrequested/conflicts separately, retain hashes and paths. Test overlap and successful collection across partial batches.
- [x] Add separate R5 changepack and user-facing instructions. Preserve R4 entry.
- [x] Add pre-build locked metadata CI and same-commit release synchronization; verify the next patch bump without external dependency churn.
- [x] Run cargo test --workspace -j 2 --no-fail-fast, clippy --workspace --all-targets -j 2 -- -D warnings, fmt check, build --workspace -j 2. Keep logs outside target and confirm local executables.
- [x] Review diff and verify only two bounded HUG mask golden changes.
- Finish with the authorized PR #26 follow-up commit and cargo clean; retain final outcome in the external R5 verification report.
